{
  "server_url": "https://sync.dspop.info",
  "repo_path": ".",
  "sync_options": {
    "verify_checksums": true,
    "skip_existing": true,
    "chunk_size_mb": 1
  },
  "exclude_patterns": [],
  "notes": [
    "Replace 'server_url' with your Cloudflare Tunnel hostname (e.g., https://filesync.yourdomain.com)",
    "You can configure this hostname in the Cloudflare Dashboard for your 'fileserver-tunnel'",
    "repo_path can be '.' for current directory or absolute path"
  ]
}
